import './CustomButton.scss';

const CustomButton = () => {
    return (
        <div>
            <button className="cust_btn">Оставить заявку</button>
        </div>
    );
};

export default CustomButton;
